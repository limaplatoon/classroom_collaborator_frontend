import React from 'react'

const MyClassesPage = () => {
  return (
    <div>
      <h1>this is The Page where we will be able to view available class sections to join and create new classes</h1>
	  <h2>We will need to create an api endpoint to add a user to a class section on the back enc</h2>
	  <h2>We will need to create an api endpoint to create a class section and add the current user to that class section on the back end</h2>
    </div>
  )
}

export default MyClassesPage
